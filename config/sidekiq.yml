:scheduler:
  :listened_queues:
    - default
  :schedule:
    reminder_worker:
      cron: '* * * * * Asia/Jakarta'
      class: ReminderWorker

    status_worker:
      cron: '* * * * * Asia/Jakarta'
      class: StatusWorker